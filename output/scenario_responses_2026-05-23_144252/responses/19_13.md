# 19.13 — consent default false + set + revoke

- Layer: L19
- Kind: infra
- Expected behavior: default false; can set/revoke
- Ollama: ok=True, total_s=8.57, tokens=85

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: consent default false + set + revoke (layer L19)
Details: (default → set true → revoke)
Expected outcome: default false; can set/revoke

RESPOND NOW:
```

## Ollama response
This system test is checking how the CPAL personalized-learning system handles user consent for sharing data. The test starts with consent set to "false" by default, then sets it to "true", and finally revokes (or takes away) that consent.

A successful outcome means:

* Consent is initially false
* Setting consent to true works correctly
* Revoking consent returns the setting back to its original state of false
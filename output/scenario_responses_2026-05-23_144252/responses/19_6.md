# 19.6 — GDPR delete wipes all traces + tombstone

- Layer: L19
- Kind: infra
- Expected behavior: all rows gone, tombstone present
- Ollama: ok=True, total_s=9.82, tokens=100

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: GDPR delete wipes all traces + tombstone (layer L19)
Details: (populate then delete_student)
Expected outcome: all rows gone, tombstone present

RESPOND NOW:
```

## Ollama response
This system test checks how CPAL handles deleting a student's data. Here's what it does:

1. The test creates a new student record in the database.
2. It then attempts to delete this student's entire record, including all associated data.

A successful outcome looks like this:

* All records related to the deleted student are completely gone from the database.
* A "tombstone" remains, which is a marker indicating that the student was once present but has since been deleted.
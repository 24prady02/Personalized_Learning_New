# 19.9 — auth token rejects tampering

- Layer: L19
- Kind: infra
- Expected behavior: altered token → None
- Ollama: ok=True, total_s=10.18, tokens=98

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: auth token rejects tampering (layer L19)
Details: (flip a byte in the middle of a valid token)
Expected outcome: altered token → None

RESPOND NOW:
```

## Ollama response
We're testing how our system handles attempts to manipulate an authentication token. In this case, we're simulating a user trying to alter a valid token by changing one of its bytes.

A successful outcome looks like this:

When the system detects that the token has been tampered with, it should immediately reject and return `None`, indicating that the altered token is no longer valid for authentication purposes. This ensures our system remains secure against potential attempts to compromise user accounts or access unauthorized features.
# 0.18 — missing API fields

- Layer: L0
- Kind: infra
- Expected behavior: graceful when fields absent
- Ollama: ok=True, total_s=7.98, tokens=90

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: missing API fields (layer L0)
Details: (session_data has only the question key, all else missing)
Expected outcome: graceful when fields absent

RESPOND NOW:
```

## Ollama response
We're running a test to see how our system behaves when it's missing some crucial information from an external source. Specifically, we want to make sure that CPAL can still function smoothly even if it doesn't receive the usual details about the student's session.

A successful outcome looks like this: the system doesn't crash or behave erratically, and instead simply skips over the missing fields without impacting the rest of the learning experience for our students.
# 0.19 — null API fields

- Layer: L0
- Kind: infra
- Expected behavior: graceful when fields are None
- Ollama: ok=True, total_s=7.25, tokens=80

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: null API fields (layer L0)
Details: (every session_data field is explicitly null)
Expected outcome: graceful when fields are None

RESPOND NOW:
```

## Ollama response
This internal test checks how the CPAL system handles empty or missing data. Specifically, it's testing if the system can still function correctly even when certain key fields are intentionally left blank. 

A successful outcome means that the system doesn't break or crash due to these missing fields and instead responds appropriately, possibly by defaulting to a standard behavior or displaying an error message clearly indicating what went wrong.
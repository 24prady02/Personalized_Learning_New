# 8.5 — across-skill independence (per-skill prior)

- Layer: L8
- Kind: infra
- Expected behavior: overloading stays at its own prior
- Ollama: ok=True, total_s=10.67, tokens=113

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: across-skill independence (per-skill prior) (layer L8)
Details: (3 correct on null_pointer; check overloading untouched)
Expected outcome: overloading stays at its own prior

RESPOND NOW:
```

## Ollama response
This internal test is checking how the CPAL system handles different skills that are related but still independent of each other. Specifically, it's testing whether the system correctly maintains a student's prior knowledge in one skill when they're working on a related but separate skill.

A successful outcome looks like this: even though the student answers some questions correctly on the "null_pointer" topic, the system doesn't "overload" or transfer that knowledge to another unrelated topic. Instead, it keeps the overloading (i.e., prior knowledge) specific to its own topic.
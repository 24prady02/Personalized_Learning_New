# 19.2 — DB handles 10-thread concurrent writes

- Layer: L19
- Kind: infra
- Expected behavior: no corruption, all rows present
- Ollama: ok=True, total_s=9.72, tokens=99

## Prompt sent to Ollama
```
SYSTEM:
You are an explainer for the CPAL personalized-learning system. The following is an INTERNAL system test (not a real student message). Briefly describe in plain English what is being tested and what a successful outcome looks like, in <= 120 words.

INTERNAL TEST: DB handles 10-thread concurrent writes (layer L19)
Details: (10 threads × 50 upserts each)
Expected outcome: no corruption, all rows present

RESPOND NOW:
```

## Ollama response
This internal test is checking how our database system performs when multiple users are writing data to it at the same time. Specifically, we're simulating 10 different users making 50 changes each to the database simultaneously.

A successful outcome means that:

* The database doesn't get corrupted or mixed up
* All of the changes made by each user are recorded correctly in the database

In other words, the test ensures that our system can handle a heavy load of concurrent writes without any errors.
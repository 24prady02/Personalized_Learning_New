# CPAL environment variables
# Copy this file to .env and fill in values

# No API key needed when using Ollama (local LLM)
# Just make sure Ollama is running: ollama serve

# Optional — only needed if you switch back to cloud LLMs later
# ANTHROPIC_API_KEY=sk-ant-...
# GROQ_API_KEY=gsk_...
